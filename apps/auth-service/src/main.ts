/**
 * This is not a production server yet!
 * This is only a minimal backend to get started.
 */

import { bootstrap } from '@org/bootstrap';
import { AppModule } from './app/app.module';

bootstrap(AppModule, 'Auth Service');
